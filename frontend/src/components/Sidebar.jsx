import { BarChart3, BrainCircuit, Database, FileCode2, Gauge, Layers3, Settings, X } from 'lucide-react'
import { NavLink } from 'react-router-dom'

const links = [['/', Gauge, 'Overview'], ['/query', BrainCircuit, 'AI Query'], ['/analytics', BarChart3, 'Analytics'], ['/metrics', Layers3, 'Metrics'], ['/dimensions', Layers3, 'Dimensions'], ['/dataset', Database, 'Dataset'], ['/sql', FileCode2, 'SQL Explorer'], ['/settings', Settings, 'Settings']]
export default function Sidebar({ open, onClose }) { return <aside className={`sidebar ${open ? 'open' : ''}`}><div className="brand"><div className="brand-mark"><BrainCircuit size={20} /></div><span>Metric<span>Mind</span></span><button className="icon-button sidebar-close" onClick={onClose}><X size={18} /></button></div><nav>{links.map(([to, Icon, label]) => <NavLink key={to} to={to} end={to === '/'} onClick={onClose}><Icon size={18} /><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><div className="status-dot" /><div><strong>Backend connected</strong><small>SQLite workspace</small></div></div></aside> }
