import { useState } from 'react'
import { ArrowRight, BrainCircuit, Eye, EyeOff, ShieldCheck } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/useAuth'
export default function Login() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [show, setShow] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const { login } = useAuth(); const navigate = useNavigate(); const submit = (event) => { event.preventDefault(); if (!email || !password) return setError('Enter your email and password to continue.'); setLoading(true); setTimeout(() => { login(email); navigate('/'); }, 450) }; return <main className="auth-page"><div className="auth-art"><div className="auth-art-inner"><div className="brand auth-brand"><div className="brand-mark"><BrainCircuit size={20} /></div><span>Metric<span>Mind</span></span></div><div className="art-copy"><p className="eyebrow">AI-POWERED BUSINESS INTELLIGENCE</p><h1>Find the signal<br /><em>inside your data.</em></h1><p>Ask sharper questions. Make decisions with evidence.</p></div><div className="signal-card"><ShieldCheck size={18} /><span>Private workspace analytics</span><b>Live</b></div></div></div><div className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><p className="eyebrow">WELCOME BACK</p><h2>Sign in to your workspace</h2><p className="muted">Your next clear decision starts here.</p>{error && <div className="error-box">{error}</div>}<label>Email address<input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" /></label><label>Password<div className="password-input"><input type={show ? 'text' : 'password'} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password" /><button type="button" onClick={() => setShow(!show)} aria-label="Toggle password visibility">{show ? <EyeOff size={17} /> : <Eye size={17} />}</button></div></label><div className="form-row"><label className="checkbox"><input type="checkbox" /> Remember me</label><a href="#forgot">Forgot password?</a></div><button className="primary-button full" disabled={loading}>{loading ? 'Signing in...' : <>Enter workspace <ArrowRight size={17} /></>}</button><p className="auth-switch">New to MetricMind? <Link to="/register">Create an account</Link></p><small className="auth-note">Authentication UI is local until backend auth endpoints are available.</small></form></div></main> }
