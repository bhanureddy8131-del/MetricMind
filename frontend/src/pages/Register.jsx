import { useState } from 'react'
import { ArrowRight, BrainCircuit } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { useAuth } from '../context/useAuth'
export default function Register() { const [form, setForm] = useState({ name: '', email: '', password: '', confirm: '' }); const [error, setError] = useState(''); const { login } = useAuth(); const navigate = useNavigate(); const update = (key) => (e) => setForm({ ...form, [key]: e.target.value }); const submit = (e) => { e.preventDefault(); if (Object.values(form).some((value) => !value)) return setError('Complete every field to continue.'); if (form.password.length < 8) return setError('Use at least 8 characters for your password.'); if (form.password !== form.confirm) return setError('Passwords do not match.'); login(form.email, form.name); navigate('/'); }; return <main className="auth-page"><div className="auth-art"><div className="auth-art-inner"><div className="brand auth-brand"><div className="brand-mark"><BrainCircuit size={20} /></div><span>Metric<span>Mind</span></span></div><div className="art-copy"><p className="eyebrow">A CLEARER WAY TO WORK</p><h1>Make every<br /><em>metric count.</em></h1><p>Bring your business questions and your data into one calm workspace.</p></div></div></div><div className="auth-form-wrap"><form className="auth-form" onSubmit={submit}><p className="eyebrow">GET STARTED</p><h2>Create your workspace</h2><p className="muted">A local profile for this frontend workspace.</p>{error && <div className="error-box">{error}</div>}<label>Full name<input value={form.name} onChange={update('name')} placeholder="Alex Morgan" /></label><label>Email address<input type="email" value={form.email} onChange={update('email')} placeholder="you@company.com" /></label><label>Password<input type="password" value={form.password} onChange={update('password')} placeholder="8+ characters" /></label><label>Confirm password<input type="password" value={form.confirm} onChange={update('confirm')} placeholder="Repeat your password" /></label><button className="primary-button full">Create workspace <ArrowRight size={17} /></button><p className="auth-switch">Already have an account? <Link to="/login">Sign in</Link></p><small className="auth-note">Backend authentication is not currently exposed by the API.</small></form></div></main> }
